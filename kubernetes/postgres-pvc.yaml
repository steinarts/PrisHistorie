apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prishistorie-postgres-data
  namespace: prishistorie
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi

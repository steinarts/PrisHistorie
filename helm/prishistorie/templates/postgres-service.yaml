{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: prishistorie-postgres
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: prishistorie-postgres

  ports:
  - port: {{ .Values.postgres.service.port }}
    targetPort: {{ .Values.postgres.service.port }}
{{- end }}